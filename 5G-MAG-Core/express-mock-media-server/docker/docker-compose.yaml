version: '3.8'
services:
  express-mock-media-server:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    ports:
      - "3004:3004"
    environment:
      - PORT=3004
    restart: unless-stopped
